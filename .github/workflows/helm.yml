name: Release Helm Charts
on:
  push:
    tags:
      - 'multi-john-*.*.*'
jobs:
  call-helm-release:
    permissions:
      contents: write
    uses: adamanteye/actions/.github/workflows/helm.yml@master
